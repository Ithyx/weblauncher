[package]
name = "weblauncher"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", features = [ "derive" ] }
dirs = "6.0"

rocket = "0.5"

serde = "1.0"
toml = "0.8"
